<div id="<%= dom_id image %>">
    <p>
      <strong>Name:</strong>
      <%= image.name %>
    </p>

    <p>
      <%= image_tag(image.picture.url, size: "300x300") if image.picture? %>
    </p>

    <p>
      <strong>User:</strong>
      <%= image.user_id %>
    </p>

  </div>